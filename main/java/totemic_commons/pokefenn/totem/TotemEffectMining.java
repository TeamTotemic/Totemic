package totemic_commons.pokefenn.totem;

import totemic_commons.pokefenn.api.ITotemEffect;

/**
 * Created with IntelliJ IDEA.
 * User: Pokefenn
 * Date: 21/02/14
 * Time: 16:52
 */
public class TotemEffectMining// implements ITotemEffect
{
    /*

    public static void effect(TileTotemIntelligence totem, int i, int upgrades)
    {
        int RANGE_X = 3 + upgrades;
        int RANGE_Z = 3 + upgrades;

        //System.out.println(":O");

        //System.out.println("xD");
        for (int j = -RANGE_X; j <= RANGE_X + upgrades; j++)
        {
            for (int k = 1; k <= totem.yCoord; k++)
            {
                for (int l = -RANGE_Z; l <= RANGE_Z + upgrades; l++)
                {    //System.out.println(":P");
                    if (totem.worldObj.getBlockId(totem.xCoord + j, totem.yCoord - 4 - k, totem.zCoord + l) != Block.bedrock.blockID && totem.worldObj.getBlockMaterial(totem.xCoord + j, totem.yCoord - 4 - k, totem.zCoord + l) != Material.water && totem.worldObj.getBlockMaterial(totem.xCoord + j, totem.yCoord - 4 - k, totem.zCoord + l) != Material.lava)
                    {
                        if (totem.worldObj.getWorldTime() % 100L == 0)
                        {

                            //System.out.println((totem.xCoord + j) + (totem.yCoord - 4 - k) + (totem.zCoord + l));

                            totem.worldObj.destroyBlock(totem.xCoord + j, totem.yCoord - 4 - k, totem.zCoord + l, false);

                            totem.decreaseChlorophyll(TotemUtil.decrementAmount(i));
                        }
                    }

                }

            }

        }

    }
    */

}


